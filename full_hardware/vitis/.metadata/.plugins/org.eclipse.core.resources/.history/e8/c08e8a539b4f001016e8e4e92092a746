#ifndef STATES_H
#define STATES_H

#include <stdint.h>
#include <stdbool.h>

typedef struct
{
    State *prev;   // Puntero al estado anterior
    int state_id;
    int options[4];
    int option_number;
} State ;

typedef enum{
	ST_SETUP = 0,
	ST_BV,
	ST_BV_MENU

} STATE_ID;

#endif
